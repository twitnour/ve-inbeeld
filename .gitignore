# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# PHP backend (backend/) — real SMTP credentials and Composer output
# must never be committed. composer.lock IS tracked (no secrets, keeps
# installs reproducible) — only vendor/ and the real config.php aren't.
backend/vendor/
backend/config.php
.env
